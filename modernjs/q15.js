const name = "John";
const age = 25;

// Object shorthand
const person = { name, age };

// Template literal
console.log(`Person's name is ${person.name} and age is ${person.age}`);
// Output: Person's name is John and age is 25